# URL base del Backend API (FastAPI). Es la única variable que necesita el frontend.
# Copia este archivo como `.env.local` y ajusta el valor si el backend no corre en el puerto 8000.
VITE_API_URL=http://localhost:8000
